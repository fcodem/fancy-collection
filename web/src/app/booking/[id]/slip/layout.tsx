import type React from "react";

export default function BookingSlipLayout({ children }: { children: React.ReactNode }) {
  return <div style={{ background: "#e8e8e8", padding: 0 }}>{children}</div>;
}
