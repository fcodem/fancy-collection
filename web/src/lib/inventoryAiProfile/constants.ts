/** AI Inventory Profile pipeline version — bump when profile schema or job logic changes. */
export const AI_PROFILE_PIPELINE_VERSION = "1";

/** Max background retries for profile generation. */
export const AI_PROFILE_MAX_RETRIES = 3;

/** Vision model identifier (provider-agnostic label stored in history). */
export const AI_PROFILE_VISION_MODEL = "claude-sonnet-4-6";
